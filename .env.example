# Supabase — get values from the Supabase project dashboard (Settings → API).
# Only the publishable/anon key belongs in a VITE_-prefixed var; it ships to the browser.
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
