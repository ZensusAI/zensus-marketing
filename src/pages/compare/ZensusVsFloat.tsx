import { ComparePageLayout } from "@/components/compare/ComparePageLayout";
import { FLOAT_COMPARE } from "@/lib/compare-pages";

const ZensusVsFloat = () => <ComparePageLayout config={FLOAT_COMPARE} />;

export default ZensusVsFloat;
