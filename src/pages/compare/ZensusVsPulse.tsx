import { ComparePageLayout } from "@/components/compare/ComparePageLayout";
import { PULSE_COMPARE } from "@/lib/compare-pages";

const ZensusVsPulse = () => <ComparePageLayout config={PULSE_COMPARE} />;

export default ZensusVsPulse;
