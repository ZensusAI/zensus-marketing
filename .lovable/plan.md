

# Remove Integration Sections from Landing Page

## Changes

### 1. `src/pages/Index.tsx`
- Remove the `IntegrationLogos` and `IntegrationBanner` imports and their usage from the page layout.

### 2. Optional cleanup
- `src/components/landing/IntegrationLogos.tsx` and `src/components/landing/IntegrationBanner.tsx` can be deleted since they're no longer used.

