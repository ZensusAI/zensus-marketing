# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — configure via Vercel env vars, never commit
.env
.env.*
!.env.example

# Lovable artifacts (historical — platform ejected)
.lovable/

# Lockfile policy — this project uses npm (package-lock.json)
bun.lock
bun.lockb
pnpm-lock.yaml
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
