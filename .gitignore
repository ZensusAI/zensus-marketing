# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment files — configure via Vercel env vars, never commit
.env
.env.*
*.env
*.env.*
.envrc
!.env.example

# Secrets, keys, certificates — never commit
*.pem
*.key
*.cer
*.crt
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
*.asc
*.gpg
secrets/
secret/
credentials/
credentials.json
service-account*.json
gcp-*.json
firebase-adminsdk*.json
.npmrc
.netrc

# Supabase CLI local artifacts (may contain session/keys)
.supabase/
supabase/.branches/
supabase/.temp/

# Vercel local artifacts
.vercel

# Build caches / tooling caches
.cache/
.parcel-cache/
.turbo/
.eslintcache
.stylelintcache
*.tsbuildinfo
coverage/
.nyc_output/

# Temp / backup / patch leftovers
tmp/
temp/
*.bak
*.backup
*.tmp
*.orig
*.rej

# Lovable artifacts (historical — platform ejected)
.lovable/

# Visual companion brainstorm sessions (ephemeral)
.superpowers/

# Lockfile policy — this project uses npm (package-lock.json)
bun.lock
bun.lockb
pnpm-lock.yaml
yarn.lock

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
